---
'@nestjs-yalc/app': minor
---

Add `swaggerDocumentOptions` to `ICreateOptions` so callers can pass `SwaggerDocumentOptions` (for example `autoTagControllers: false`) to `SwaggerModule.createDocument`.
